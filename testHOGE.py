#! /usr/bin/python
# -*- coding: utf-8 -*-

import numpy as np
import matplotlib.pyplot as plt

array = np.zeros((10, 100))

print "len(array)    : %d" % len(array)
print "len(array[0]) : %d" % len(array[0])

